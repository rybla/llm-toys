{
  "name": "llm-toys",
  "scripts": {
    "build": "spago build",
    "compile": "rm -rf docs/ && spago run -m Compile",
    "serve": "bun run --hot server.ts"
  },
  "author": "Henry Blanchette",
  "license": "ISC",
  "dependencies": {
    "@types/bun": "^1.1.16",
    "esbuild": "^0.24.2",
    "openai": "^4.77.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "purescript": "^0.15.16-4",
    "spago": "^0.93.41"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}